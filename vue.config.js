module.exports = {
    configureWebpack: {
      devtool: 'source-map'
    },
    //vue tips didn't work for me
    // publicPath: process.env.NODE_ENV === 'production'
    // ? '/technical_test_social_place/'
    // : '/'

  }

